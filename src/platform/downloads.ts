// Design-OS stub for circleup's platform/downloads.
// Real implementation handles native file-save on mobile (Capacitor).
export async function saveFile(_blob: Blob, _filename: string): Promise<void> {
  // No-op in design previews.
}
