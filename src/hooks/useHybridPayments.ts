// Design-OS stub for circleup's useHybridPayments hook.
export function useHybridPayments() {
  return {
    paymentMethods: [],
    twintEnabled: true,
    loading: false,
    submitPayment: async (..._args: unknown[]) => ({ success: true }),
  }
}
