import { EventCreator } from './components'

export default function EventCreatorPreview() {
  return (
    <EventCreator
      onSave={() => {}}
      onPublish={() => {}}
      onCancel={() => {}}
    />
  )
}
